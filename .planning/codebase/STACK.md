# Technology Stack

**Analysis Date:** 2026-02-18

## Languages

**Primary:**
- Python 3.8+ - Core library implementation and tests

**Secondary:**
- None

## Runtime

**Environment:**
- Python 3.8 or higher

**Package Manager:**
- Poetry (primary build system)
- pip (for installation)
- Lockfile: `poetry.lock` (generated by Poetry, not present in repo)

## Frameworks

**Core:**
- None (pure Python standard library)

**Testing:**
- pytest 8.2.2 - Test runner and framework

**Build/Dev:**
- Poetry - Dependency management and packaging
- Build dependencies: `poetry-core`

## Key Dependencies

**Critical:**
- fnmatch - Python standard library module for wildcard matching (`import fnmatch` in `src/json_parser/__init__.py`), used for glob-style pattern matching in key extraction
- typing.Optional - Used for `max_depth: Optional[int]` parameter annotation (Python 3.8 compat)

**Testing:**
- pytest 8.2.2 - Test runner
- iniconfig 2.0.0 - pytest configuration support
- packaging 24.1 - Package version handling (transitive)
- pluggy 1.5.0 - pytest plugin system (transitive)

**Build/Packaging:**
- poetry-core - Poetry build backend for creating distributable packages

## Configuration

**Environment:**
- Python version specification: `.python-version` contains "json_parser" as directive
- No environment variables required for runtime (library is zero-dependency)

**Build:**
- `pyproject.toml` - Poetry configuration with project metadata, dependencies, and build system
- Build output: `dist/` directory contains wheel and source distributions

## Platform Requirements

**Development:**
- Python 3.8+
- Poetry (for development and building)
- pytest (for testing)

**Production:**
- Python 3.8+ only
- No external dependencies - uses only Python standard library
- Distributable as wheel (`.whl`) or source distribution (`.tar.gz`)

---

*Stack analysis: 2026-02-18*
