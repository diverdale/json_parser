---
phase: 01-docs-and-ergonomics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02

must_haves:
  truths:
    - "A developer landing on the README sees the problem statement hook within the first screen before any code"
    - "A developer can copy a working 3-line extraction example from the top of the README"
    - "A developer can see real-world examples with wildcards and duplicate-key merging inline in the README"
    - "The README opens with badges (PyPI version, Python versions, license)"
    - "Installation section shows only 'pip install json-key-parser'"
  artifacts:
    - path: "README.md"
      provides: "Complete library documentation with problem hook, quick-start, and real-world examples"
      min_lines: 120
  key_links:
    - from: "README.md quick-start"
      to: "JsonParser class"
      via: "from json_parser import JsonParser"
      pattern: "from json_parser import JsonParser"
---

<objective>
Rewrite README.md into a compelling, scannable library page that converts a landing developer into a user in under 60 seconds.

Purpose: The library currently has no hook and no compelling examples. Developers who discover it on PyPI or GitHub have no fast path to "this solves my problem."
Output: A fully rewritten README.md with problem hook, badges, quick-start using person/contact data, and inline real-world examples showing wildcards and duplicate handling.
</objective>

<execution_context>
@/Users/dalwrigh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dalwrigh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-docs-and-ergonomics/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README with problem hook, badges, and quick-start</name>
  <files>README.md</files>
  <action>
Replace the entire README.md content with the following structure (in order):

**1. Badges (top of file, before any heading)**
Three badges on a single line:
- PyPI version: `[![PyPI version](https://img.shields.io/pypi/v/json-key-parser.svg)](https://pypi.org/project/json-key-parser/)`
- Python versions: `[![Python 3.8+](https://img.shields.io/badge/Python-3.8%2B-blue.svg)](https://www.python.org/downloads/)`
- License: `[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](/LICENSE)`

**2. Project title heading**
`# json-key-parser`

**3. Problem statement hook (before any code)**
Open with a short paragraph that names the pain: drilling into `response['data'][0]['user']['address']['city']` for every key you need, writing nested loops, wrapping everything in try/except. Then one sentence pivoting to the solution. Exact wording is Claude's discretion — make it conversational and relatable. Keep it 3–5 sentences max.

**4. Quick-start section**
Heading: `## Quick Start`

Show a complete, self-contained code block using person/contact data (first_name, last_name, birthday, nested address). The example MUST be 3 lines of meaningful code after the data definition:
```python
from json_parser import JsonParser

data = [{"first_name": "Alice", "last_name": "Smith", "birthday": "1990-04-15",
         "address": {"street": "12 Oak Ave", "city": "Portland", "zip": "97201"}},
        {"first_name": "Bob", "last_name": "Jones", "birthday": "1985-09-30",
         "address": {"street": "88 Pine St", "city": "Seattle", "zip": "98101"}}]

result = JsonParser(data, ["first_name", "last_name", "birthday"]).get_data()
```

Follow the code block with the expected output JSON (formatted with indent=4).

**5. Why this library? (Features)**
Heading: `## Why json-key-parser?`

Short bullet list (4–5 bullets max):
- Declare keys, skip the traversal — one call replaces nested loops
- Wildcard patterns (`address*` matches `address`, `address1`, `address2`)
- Duplicate values merged automatically — no deduplication code needed
- Works on any nesting depth
- Zero dependencies — pure Python stdlib

**6. Wildcard example section**
Heading: `## Wildcard Matching`

Use the SAME `data` variable defined in the quick-start (tell the reader "Using the same data from above:"). Show `address*` pattern extracting address dicts. Show the output.

**7. Duplicate key merging section**
Heading: `## Duplicate Key Merging`

Create a NEW small inline data example (2 records max) where one record has both `address1` and `address2` at the top level, each containing a `street` key. Show that extracting `"street"` returns a list of two street values for that record. Show output. Do NOT reference files in examples/ folder — keep it self-contained.

**8. Installation section**
Heading: `## Installation`

Single code block: `pip install json-key-parser`
No Poetry instructions. No "install from source" instructions.

**9. License**
Heading: `## License`
One line: MIT. Link to /LICENSE file.

IMPORTANT constraints:
- Do NOT include an API Reference section enumerating methods (per user decision)
- Do NOT reference the examples/ folder anywhere
- Do NOT include Poetry installation instructions
- Do NOT include "Running Tests" section
- Keep the README scannable — prefer short paragraphs and code over long prose
  </action>
  <verify>
    1. `grep -n "Tired\|nested loop\|response\[" README.md` — confirm problem hook exists with relatable language
    2. `grep -n "pip install json-key-parser" README.md` — confirm installation line is present
    3. `grep -n "address\*" README.md` — confirm wildcard example is present
    4. `grep -n "street.*list\|street.*\[" README.md` — confirm duplicate merging output is shown (may need to check output block)
    5. `grep -n "PyPI version\|pypi/v" README.md` — confirm PyPI badge is present
    6. `wc -l README.md` — confirm file has at least 120 lines
    7. Visual check: the first code example fits within 3 meaningful lines after the import
  </verify>
  <done>
    README.md is fully rewritten with: (1) three badges at top, (2) problem hook before any code, (3) quick-start using person/contact data with 3-line extraction, (4) wildcard matching section with example, (5) duplicate key merging section with inline data, (6) installation showing only pip install, (7) no API reference table, no examples/ folder references, no Poetry instructions.
  </done>
</task>

</tasks>

<verification>
After execution:
1. Run `cat README.md | head -20` — badges and problem hook visible within first 20 lines
2. Run `grep -c "##" README.md` — confirm at least 5 section headings exist
3. Run `grep "from json_parser import JsonParser" README.md` — import line present in quick-start
4. Run `python3 -c "
import json
from sys import path
path.insert(0, 'src')
from json_parser import JsonParser
data = [{'first_name': 'Alice', 'last_name': 'Smith', 'birthday': '1990-04-15', 'address': {'street': '12 Oak Ave', 'city': 'Portland', 'zip': '97201'}}]
result = JsonParser(data, ['first_name', 'last_name', 'birthday']).get_data()
print(result)
"` — confirm the README quick-start code actually works
</verification>

<success_criteria>
- A developer landing on README sees the problem hook before any code
- Quick-start example is copy-paste runnable in 3 meaningful lines
- Wildcard and duplicate-key merging behaviors are demonstrated with output
- No method API table, no examples/ folder references, no Poetry install instructions
- README has ≥120 lines
</success_criteria>

<output>
After completion, create `.planning/phases/01-docs-and-ergonomics/01-01-SUMMARY.md` with:
- What was built (sections added, key decisions made on wording/structure)
- What the README structure looks like now
- Any deviations from the plan and why
</output>
