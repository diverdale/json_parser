---
phase: 03-quality-and-ship
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
autonomous: true
requirements:
  - QUAL-02

must_haves:
  truths:
    - "pyproject.toml version field reads 0.1.0"
    - "pyproject.toml classifiers include Python 3.8, 3.9, 3.10, 3.11, 3.12 version entries"
    - "pyproject.toml classifiers include Intended Audience :: Developers"
    - "pyproject.toml classifiers include Topic :: Software Development :: Libraries :: Python Modules"
    - "pyproject.toml classifiers include Topic :: Utilities"
    - "pyproject.toml readme field points to README.md (poetry uses this as long_description on PyPI)"
    - "pyproject.toml keywords list contains at least 8 developer-searchable terms"
    - "dist/ contains only v0.1.0 artifacts (no stale 0.0.1 or 0.0.2 files)"
    - "poetry build succeeds and produces json_key_parser-0.1.0-py3-none-any.whl and json_key_parser-0.1.0.tar.gz"
  artifacts:
    - path: "pyproject.toml"
      provides: "Complete publish-ready package metadata for v0.1.0"
      contains: "version = \"0.1.0\""
    - path: "dist/json_key_parser-0.1.0-py3-none-any.whl"
      provides: "Installable wheel artifact for PyPI upload"
    - path: "dist/json_key_parser-0.1.0.tar.gz"
      provides: "Source distribution artifact for PyPI upload"
  key_links:
    - from: "pyproject.toml"
      to: "README.md"
      via: "readme = \"README.md\" field"
      pattern: "readme.*README\\.md"
    - from: "dist/*.whl"
      to: "pyproject.toml"
      via: "poetry build reads version from pyproject.toml"
      pattern: "0\\.1\\.0"
---

<objective>
Update pyproject.toml to v0.1.0 with full PyPI metadata, remove stale dist/ artifacts, and build fresh v0.1.0 wheel and sdist — publish-ready for manual `poetry publish`.

Purpose: The package currently has minimal classifiers and an outdated version. A well-classified PyPI page is discoverable by developers searching for JSON extraction tools and displays the README as the long description automatically via the `readme` field.
Output: Updated pyproject.toml at v0.1.0 with complete classifiers/keywords, a clean dist/ containing only the v0.1.0 wheel and sdist.
</objective>

<execution_context>
@/Users/dalwrigh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dalwrigh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update pyproject.toml with v0.1.0 metadata</name>
  <files>pyproject.toml</files>
  <action>
    Edit pyproject.toml with the following changes. Make ONLY the fields listed — do not touch
    [tool.poetry.dependencies] or [build-system].

    1. version: Change from "0.0.2" to "0.1.0"

    2. description: Replace with this benefit-focused one-liner:
       "Extract specific keys from deeply nested JSON structures with wildcards and zero dependencies"

    3. readme: Already set to "README.md" — leave as-is (this makes README the PyPI long description)

    4. homepage and repository: Already set to https://github.com/diverdale/json_parser — leave as-is

    5. keywords: Replace the current list with a richer, more searchable set:
       ["json", "parser", "key", "extract", "search", "nested", "recursive", "wildcard", "fnmatch", "api", "data", "query"]

    6. classifiers: Replace the current list with the full set below (exact strings — PyPI validates these):
       [
           "Development Status :: 4 - Beta",
           "Intended Audience :: Developers",
           "License :: OSI Approved :: MIT License",
           "Operating System :: OS Independent",
           "Programming Language :: Python :: 3",
           "Programming Language :: Python :: 3.8",
           "Programming Language :: Python :: 3.9",
           "Programming Language :: Python :: 3.10",
           "Programming Language :: Python :: 3.11",
           "Programming Language :: Python :: 3.12",
           "Topic :: Software Development :: Libraries :: Python Modules",
           "Topic :: Utilities",
       ]

    The final pyproject.toml [tool.poetry] section should look like:

    ```toml
    [tool.poetry]
    name = "json-key-parser"
    version = "0.1.0"
    description = "Extract specific keys from deeply nested JSON structures with wildcards and zero dependencies"
    authors = ["Dale Wright <diverdale@gmail.com>"]
    license = "MIT"
    readme = "README.md"
    homepage = "https://github.com/diverdale/json_parser"
    repository = "https://github.com/diverdale/json_parser"
    keywords = ["json", "parser", "key", "extract", "search", "nested", "recursive", "wildcard", "fnmatch", "api", "data", "query"]
    classifiers = [
        "Development Status :: 4 - Beta",
        "Intended Audience :: Developers",
        "License :: OSI Approved :: MIT License",
        "Operating System :: OS Independent",
        "Programming Language :: Python :: 3",
        "Programming Language :: Python :: 3.8",
        "Programming Language :: Python :: 3.9",
        "Programming Language :: Python :: 3.10",
        "Programming Language :: Python :: 3.11",
        "Programming Language :: Python :: 3.12",
        "Topic :: Software Development :: Libraries :: Python Modules",
        "Topic :: Utilities",
    ]
    packages = [{include = "json_parser", from = "src"}]
    ```
  </action>
  <verify>
    Run: grep 'version = ' /Users/dalwrigh/dev/json_parser/pyproject.toml
    Expected: version = "0.1.0"

    Run: grep 'Programming Language :: Python :: 3\.' /Users/dalwrigh/dev/json_parser/pyproject.toml | wc -l
    Expected: 5 (one per Python minor version 3.8–3.12)

    Run: grep 'Topic :: Utilities' /Users/dalwrigh/dev/json_parser/pyproject.toml
    Expected: match found
  </verify>
  <done>pyproject.toml version is 0.1.0, classifiers include all 5 Python version entries plus Intended Audience :: Developers and both Topic:: classifiers, keywords list has 12 terms</done>
</task>

<task type="auto">
  <name>Task 2: Clean dist/ and build v0.1.0 artifacts</name>
  <files>dist/</files>
  <action>
    Step 1 — Remove all stale dist/ artifacts (the git status shows dist/ files as deleted, but
    any lingering files need to be gone before building):
    ```bash
    rm -f /Users/dalwrigh/dev/json_parser/dist/*
    ```

    Step 2 — Build fresh v0.1.0 wheel and sdist from the updated pyproject.toml:
    ```bash
    cd /Users/dalwrigh/dev/json_parser && poetry build
    ```

    Step 3 — Verify the output files exist and have the correct version in their filenames.

    Do NOT run poetry publish — publish is manual by the developer when ready.
    Do NOT add any CI/CD configuration (GitHub Actions deferred per user decision).
  </action>
  <verify>
    Run: ls /Users/dalwrigh/dev/json_parser/dist/
    Expected output contains:
    - json_key_parser-0.1.0-py3-none-any.whl
    - json_key_parser-0.1.0.tar.gz
    No 0.0.1 or 0.0.2 files present.

    Run: poetry version --directory /Users/dalwrigh/dev/json_parser
    Expected: json-key-parser 0.1.0
  </verify>
  <done>dist/ contains exactly two files — the v0.1.0 wheel and sdist — with no stale artifacts from prior versions</done>
</task>

</tasks>

<verification>
Run the full test suite to confirm the metadata-only changes did not break anything:

```bash
cd /Users/dalwrigh/dev/json_parser && pytest tests/ -v
```

All 26+ tests must pass. Then confirm build artifacts:

```bash
ls /Users/dalwrigh/dev/json_parser/dist/
```

Confirm only v0.1.0 files are present. The package is now publish-ready — the developer runs
`poetry publish` manually when satisfied.
</verification>

<success_criteria>
- pyproject.toml version = "0.1.0"
- classifiers include Python 3.8, 3.9, 3.10, 3.11, 3.12 specific version entries
- classifiers include "Intended Audience :: Developers", "Topic :: Software Development :: Libraries :: Python Modules", "Topic :: Utilities"
- keywords list has 12 searchable terms
- dist/ contains json_key_parser-0.1.0-py3-none-any.whl and json_key_parser-0.1.0.tar.gz
- No stale 0.0.x artifacts in dist/
- All tests still pass
- `poetry publish` can be run by developer when ready (NOT run in this phase)
</success_criteria>

<output>
After completion, create `.planning/phases/03-quality-and-ship/03-02-SUMMARY.md` with:
- Final pyproject.toml version and classifier count
- dist/ artifact filenames
- Note that publish was NOT triggered — developer runs manually
</output>
